query GetPersonById($id: ID!) {
    person(id: $id) {
        id
        friends {
            id
            username
            firstName
            lastName
            email
        }
        username
        firstName
        lastName
        email
    }
}